# 01-架构设计

本页按当前仓库真实目录和公开头文件描述系统结构，不再引用旧的 `client.get()` / `pipeline()` 风格 async API。

## 目录与职责

| 目录 / 文件 | 角色 | 关键符号 |
|---|---|---|
| `galay-redis/base/` | 通用值类型、错误、基础概念 | `RedisValue`、`RedisError`、`RedisConfig`、`RedisBase` |
| `galay-redis/protocol/` | RESP 编解码与命令构建 | `RespEncoder`、`RespParser`、`RedisCommandBuilder` |
| `galay-redis/async/RedisClient.h` | 单连接 async 客户端 | `RedisClientBuilder`、`RedisClient` |
| `galay-redis/async/RedisConnectionPool.h` | 连接池 | `ConnectionPoolConfig`、`RedisConnectionPool` |
| `galay-redis/async/RedisTopologyClient.h` | 主从 / Sentinel / Cluster | `RedisMasterSlaveClient`、`RedisClusterClient` |
| `galay-redis/sync/` | 源码树中的遗留同步 API（不进入安装合同） | `RedisSession` |
| `galay-redis/module/` | C++23 模块导出 | `galay.redis`、`galay-redis-modules` |
| `examples/include/` | 头文件消费示例 | `E1`、`E2`、`E3` |
| `examples/import/` | 模块消费示例 | `E1`、`E2`、`E3` |
| `test/` | 行为验证与 API 形状验证 | `T1`~`T24` |
| `benchmark/` | benchmark 可执行文件 | `B1-redis_client_bench`、`B2-connection_pool_bench` |

## 单连接 async 执行链路

当前主执行链路如下：

1. 用 `RedisCommandBuilder` 生成 `RedisEncodedCommand` 或 `std::span<const RedisCommandView>`
2. `RedisClient::command(...)` / `RedisClient::batch(...)` 发送 RESP 数据
3. `protocol::RespParser` 解析服务器返回
4. 调用侧通过 `RedisValue` 做类型判断与取值

对应源码：

- 命令构建：`galay-redis/protocol/Builder.h`
- async 客户端：`galay-redis/async/RedisClient.h`
- RESP 解析：`galay-redis/protocol/RedisProtocol.h`
- 返回值：`galay-redis/base/RedisValue.h`

## 拓扑层

### 主从 / Sentinel

`RedisMasterSlaveClient` 负责：

- `connectMaster(...)`：显式接入主节点
- `addReplica(...)`：注册副本连接
- `addSentinel(...)` / `refreshFromSentinel()`：从 Sentinel 拉取最新地址
- `execute(cmd, args, prefer_read, auto_retry)`：按读写偏好选择连接
- `batch(commands, prefer_read)`：在主节点或副本上批量发送

真实验证来源：

- API 形状与基本行为：`test/T11-topology_and_pubsub.cc`
- 并发刷新去重：`test/T12-topology_singleflight.cc`
- 真实 failover：`test/T13-integration_cluster_sentinel.cc`

### Cluster

`RedisClusterClient` 负责：

- `addNode(...)`：添加 seed 节点
- `refreshSlots()`：刷新槽位映射
- `keySlot(key)`：计算 key slot
- `execute(cmd, args, routing_key, auto_retry)`：按 `routing_key` 路由
- `batch(commands, routing_key)`：把一批命令打到同一路由节点

真实验证来源：

- key slot 与基本路由：`test/T11-topology_and_pubsub.cc`
- MOVED / ASK / slot refresh：`test/T13-integration_cluster_sentinel.cc`

## 连接池层

`RedisConnectionPool` 基于 `RedisClient` 提供：

- 初始化与预热：`initialize()`、`warmup()`
- 获取/归还：`acquire()`、`release(...)`
- 健康检查与清理：`triggerHealthCheck()`、`triggerIdleCleanup()`
- 统计：`getStats()`

验证来源：

- 功能与 RAII：`test/T4-connection_pool.cc`
- 压测：`benchmark/B2-connection_pool_bench.cc`

## 模块与目标

仓库中真实存在的核心 CMake 目标：

- `galay-redis`
- `galay-redis-modules`（仅在模块工具链条件满足时生成）
- `E1-async_basic_demo`
- `E2-pipeline_demo`
- `E3-topology_pubsub_demo`
- `B1-redis_client_bench`
- `B2-connection_pool_bench`
- `T1-async` ~ `T19-rediss_pool_and_topology`

模块接口文件：

- `galay-redis/module/galay.redis.cppm`
- `galay-redis/module/ModulePrelude.hpp`

## 生命周期与并发边界

### 调度器绑定

`RedisClientBuilder`、`RedisMasterSlaveClientBuilder`、`RedisClusterClientBuilder` 都要求显式注入 `IOScheduler*`。示例和测试统一通过 `Runtime::getNextIOScheduler()` 取调度器。

### 移动语义限制

`RedisClient` 头文件明确写了两条限制：

- 不要在操作进行中移动 `RedisClient`
- 确保所有 awaitable 已经失效后再移动

因此文档不再把 `RedisClient` 描述为“可随意移动的轻量句柄”。

### Pub/Sub 读取路径

Pub/Sub 订阅后，真正读取消息使用的是：

```cpp
co_await subscriber.receive(1)
```

而不是某个专门的 `subscribeMessage()` 辅助 API。真实示例见 `examples/include/E3-topology_pubsub_demo.cc` 和 `test/T11-topology_and_pubsub.cc`。

### 拓扑刷新 single-flight

`refreshFromSentinel()` 和 `refreshSlots()` 的并发去重行为由 `test/T12-topology_singleflight.cc` 覆盖：同一波突发请求只会触发一次后台刷新。

## 文档使用建议

- 要查签名：看 [02-API参考](02-API参考.md)
- 要查迁移：看 [03-使用指南](03-使用指南.md)
- 要查可运行命令：看 [04-示例代码](04-示例代码.md)
- 要查 benchmark：看 [05-性能测试](05-性能测试.md)
