# 11-协程

本页现在只保留 C++ 协程语法在 `galay-kernel` 中的落地点；完整工作流请优先回到主干页。

## 本页回答什么

- 仓库里的公开任务入口是什么
- `co_await task`、`Task<void>::then(...)`、`sleep(...)` 这些能力先看哪里
- 哪些测试 / 示例最适合作为语义锚点

## 当前稳定事实

- 公开返回类型统一为 `Task<T>`
- 等待子任务统一写成 `co_await childTask()`
- 根任务串接统一写成 `task.then(nextTask)`
- `sleep(...)` 依赖运行时中的全局 `TimerScheduler`；涉及时间系统时不要只看本页，要连同主干页一起看
- 子任务完成后，等待者会沿其所属 scheduler 的调度路径恢复，而不是直接跨线程恢复底层句柄

## 先看主干页

- 架构与时间系统：`docs/01-架构设计.md`
- API 与所有权语义：`docs/02-API参考.md`
- 最小 task 工作流：`docs/03-使用指南.md`
- 高频误区：`docs/07-常见问题.md`

## 源码 / 验证锚点

- 源码：`galay-kernel/kernel/Task.h`、`galay-kernel/kernel/Task.cc`
- 运行时相关：`galay-kernel/kernel/Runtime.h`、`galay-kernel/kernel/TimerScheduler.h`
- 测试：`test/T1-task_chain.cc`、`test/T20-spawn_simple.cc`、`test/T54-task_then_compat.cc`
- 示例：`examples/include/E4-task_basic.cc`、`examples/import/E4-task_basic.cc`、`examples/import/E9-timer_sleep.cc`

## RAG 关键词

- `Task`
- `Task<void>::then`
- `co_await`
- `sleep`
- `TimerScheduler`
